# 🔐 VARIABLES D'ENVIRONNEMENT BENILINK

# ⚠️ ATTENTION: Variables PUBLIQUES (exposées dans le bundle frontend)
# Toute variable commençant par VITE_ sera visible côté client
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_...

# 🔒 VARIABLES PRIVÉES (uniquement disponibles côté serveur)
# Ces variables sont sécurisées et ne sont PAS exposées dans le bundle
# À configurer dans les paramètres Vercel ou dans .env.local (non commité)

# Stripe (paiements)
STRIPE_SECRET_KEY=sk_test_votre_cle_secrete_stripe
STRIPE_PUBLISHABLE_KEY=pk_test_votre_cle_publique_stripe

# PayPal (paiements)
PAYPAL_CLIENT_ID=votre_client_id_paypal

# Gemini AI (Google - maintenant côté serveur)
GEMINI_API_KEY=votre_cle_api_gemini

# Resend (emails transactionnels)
RESEND_API_KEY=re_...

# Admin dashboard (token d'accès privé)
ADMIN_DASH_TOKEN=change-me

# Supabase (en option pour la base de données)
SUPABASE_URL=https://xxxx.supabase.co
SUPABASE_SERVICE_ROLE=your_service_role_key

# Configuration des redirections Stripe
APP_BASE_URL=https://votre-site.vercel.app
STRIPE_SUCCESS_PATH=/?checkout=success
STRIPE_CANCEL_PATH=/?checkout=cancel

# Taux de conversion (optionnel)
FCFA_PER_EUR=655

# ℹ️ NOTES:
# - Ne JAMAIS commiter ce fichier avec de vraies clés
# - Sur Vercel: Ajouter les variables dans Settings > Environment Variables
# - En local: Créer .env.local avec ces variables (déjà dans .gitignore)
# - VITE_ = PUBLIC (client), sans préfixe = PRIVÉ (serveur uniquement)

